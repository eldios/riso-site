# riso-site

The landing page served at riso.re, built with zola and deployed to GitHub
Pages by the `pages` workflow on every push to `main`. The `riso` framework
itself lives at https://github.com/eldios/riso. The theme catalog and its
gallery are a separate repository, https://github.com/eldios/riso-themes.
